---
# DEV-ONLY placeholders for the local lab. Never commit real credentials.
# Production: encrypt with Sealed Secrets / SOPS / Vault — see
# docs/secrets-management.md.
apiVersion: v1
kind: Secret
metadata:
  name: app2-secrets
  namespace: app2
type: Opaque
stringData:
  MONGODB_USERNAME: "admin"
  MONGODB_PASSWORD: "changeme"
  JWT_SECRET: "changeme-jwt-secret"
  SESSION_SECRET: "changeme-session-secret"
